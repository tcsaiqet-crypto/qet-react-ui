# Phase 03: Understanding Lifecycle Alignment (G3)
Ensure `/api/v1/runs/{id}/understanding` returns `status: "ready"` strictly when `state.understanding` exists and `state.status == "understanding_ready"`. Interim processing states return `status: "running"`.
