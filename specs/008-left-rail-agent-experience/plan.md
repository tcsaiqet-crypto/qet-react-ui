# Architecture & Plan 008: Left Rail Experience

1. Integrate Left Rail component in `App.tsx` layout shell.
2. Bind timeline state to `/runs/{id}/status` polling updates.
3. Add step animations and status icons for active and completed agents.
