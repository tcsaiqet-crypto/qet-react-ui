# Constitution 002: Feature-Driven Spec-Kit Rules

1. **No Fictitious Selectors**: Application Understanding must only record selectors actually present in the source files.
2. **Deterministic UI State**: The React UI must display real-time status from backend `/runs/{id}/status`.
3. **Traceability**: Requirements extracted must link directly to discovered controls and flows.
