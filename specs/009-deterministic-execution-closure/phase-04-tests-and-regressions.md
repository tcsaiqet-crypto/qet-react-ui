# Phase 04: Tests and Regressions (G4)
Behavior-driven tests in `frontend_behavior.test.tsx` and `test_fastapi_app.py`:
- Staged hero transitions and upload compaction
- Live processing activity rendering
- Run restore and report download artifact serving
